#version 330 core

uniform mat4 model;
uniform mat4 view;
uniform mat4 projection;

layout (location = 0) in vec3 aPos;

out vec3 vertPos;

void main()
{
   vertPos = aPos;
   vec4 pos = projection * view * vec4(aPos, 1.0);
   gl_Position = pos.xyww; // 使得深度变为1.0
}