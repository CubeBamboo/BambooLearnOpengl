#version 330 core

uniform float time;
uniform sampler2D screenTexture;
in vec2 TexCoords;

out vec4 FragColor;

void main()
{
	vec3 col = texture(screenTexture, TexCoords).rgb;
    FragColor = vec4(col, 1.0);
}